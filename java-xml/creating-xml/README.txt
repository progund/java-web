To compile and run, you could use the following command-line:
javac CreateCV.java && java -cp .:sqlite.jar CreateCV && cat CV.xml

This assumes you create the XML document with the name CV.xml in the current directory, of course.
